# 🐧 دليل البناء عبر WSL

## 📋 المتطلبات

- Windows 10/11
- WSL (Windows Subsystem for Linux)
- Ubuntu (موصى به)

---

## 🚀 الخطوات

### **1. تثبيت WSL:**

#### **الطريقة 1: من PowerShell (كمدير)**
```powershell
wsl --install
```

#### **الطريقة 2: من Microsoft Store**
- ابحث عن "Ubuntu"
- اضغط "تثبيت"

---

### **2. تثبيت Buildozer في WSL:**

افتح Ubuntu واكتب:

```bash
# تحديث النظام
sudo apt update

# تثبيت Python و Git
sudo apt install -y python3-pip git

# تثبيت Buildozer
pip3 install buildozer
```

---

### **3. نسخ المشروع:**

```bash
# الانتقال لمجلد المشروع
cd /mnt/c/Users/LENOVO/OneDrive/Desktop/adr_maintenance_system/kivy_app
```

---

### **4. بناء APK:**

#### **الطريقة 1: استخدام السكريبت**
```bash
chmod +x بناء_APK_عبر_WSL.sh
./بناء_APK_عبر_WSL.sh
```

#### **الطريقة 2: يدوياً**
```bash
# تنظيف
buildozer android clean

# بناء
buildozer android debug
```

---

## ⏳ الوقت المتوقع

- **التثبيت:** 10-15 دقيقة
- **البناء:** 10-20 دقيقة

---

## 📁 موقع APK

بعد البناء، سيكون APK في:
```
bin/adrmaintenance-0.1-arm64-v8a-debug.apk
```

---

## ⚠️ المشاكل الشائعة

### **1. Buildozer لا يعمل:**
```bash
# إعادة التثبيت
pip3 install --upgrade buildozer
```

### **2. مشاكل في التبعيات:**
```bash
# تثبيت التبعيات المطلوبة
sudo apt install -y python3-dev python3-pip git unzip openjdk-11-jdk
```

### **3. مشاكل في الصلاحيات:**
```bash
# إعطاء صلاحيات التنفيذ
chmod +x بناء_APK_عبر_WSL.sh
```

---

## ✅ الخلاصة

**الخطوات:**
1. تثبيت WSL
2. تثبيت Buildozer
3. نسخ المشروع
4. بناء APK

**الوقت:** 30-40 دقيقة (أول مرة)

---

## 💡 نصيحة

**للحل السريع:** استخدم PWA بدلاً من APK!





















